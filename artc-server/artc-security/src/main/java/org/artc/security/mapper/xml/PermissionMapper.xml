<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.artc.security.mapper.PermissionMapper">
    <select id="findPermissionSetByRoleId" resultType="org.artc.security.entity.Permission" parameterType="java.lang.String">
        SELECT
            id,
            code,
            name,
            created,
            edited,
            creator,
            editor
        FROM
            sys_permission AS permission
        LEFT JOIN
            sys_role_permission AS role_permission
        ON
            permission.id = role_permission.permission_id
        WHERE
            role_permission.role_id = #{roleId}
        AND
            permission.deleted = 0
    </select>
</mapper>