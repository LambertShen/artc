<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.artc.core.mapper.PermissionMapper">

    <sql id="permissionColumns">
            id,
            code,
            name,
            created,
            edited,
            creator,
            editor
    </sql>

    <select id="findPermissionSetByRoleId" resultType="org.artc.core.entity.Permission" parameterType="java.lang.String">
        SELECT
            <include refid="permissionColumns"/>
        FROM
            sys_permission AS permission
        LEFT JOIN
            sys_role_permission AS role_permission
        ON
            permission.id = role_permission.permission_id
        WHERE
            role_permission.role_id = #{roleId}
        AND
            permission.deleted = 0
    </select>

    <select id="findById" resultType="org.artc.core.entity.Permission">
        SELECT
            <include refid="permissionColumns"/>
        FROM
            sys_permission
        WHERE
            id = #{id}
    </select>

    <select id="findAll" resultType="org.artc.core.entity.Permission">
        SELECT
            <include refid="permissionColumns"/>
        FROM
            sys_permission
        WHERE
            deleted = 0
    </select>

    <insert id="insert">
        INSERT INTO sys_permission
            (
                id,
                code,
                name,
                created,
                creator
            )
        VALUES
            (
                #{id},
                #{code},
                #{name},
                #{created},
                #{creator}
            )
    </insert>

    <update id="update">
        UPDATE sys_permission
        <set>
            <if test="code != null">code=#{code},</if>
            <if test="name != null">name,=#{name},</if>
            <if test="edited != null">edited=#{edited}</if>
            <if test="editor != null">editor=#{editor}</if>
        </set>
        WHERE
        id = #{id}
    </update>

    <update id="delete">
        UPDATE
            sys_permission
        SET
            deleted = 1
        WHERE
            id = #{id}
    </update>
</mapper>